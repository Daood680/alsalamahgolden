'use client';
import { Project } from '@/data/projects';
import Image from 'next/image';
import { motion } from 'framer-motion';
import Link from 'next/link';
export default function ProjectGrid({ projects }: { projects: Project[] }) { return (<div className="grid gap-7 md:grid-cols-2 lg:grid-cols-3">{projects.map((p, i) => (<motion.div key={p.id} initial={{ opacity:0, y:40 }} whileInView={{ opacity:1, y:0 }} viewport={{ once:true, margin:'-80px' }} transition={{ delay:i * 0.08, duration:.6, ease:'easeOut' }} className="group relative rounded-lg overflow-hidden bg-[#121b22] border border-white/5 shadow-card"><div className="relative h-60"><Image src={p.cover} alt={p.title} fill priority={i < 2} sizes="(max-width:768px) 100vw, (max-width:1200px) 33vw" className="object-cover transition duration-700 group-hover:scale-110" /><div className="absolute inset-0 bg-gradient-to-t from-black/80 via-black/10 to-transparent opacity-80 group-hover:opacity-90 transition" /><div className="absolute bottom-4 right-4 left-4"><h3 className="text-lg font-semibold text-gold drop-shadow">{p.title}</h3><p className="text-xs text-neutral-300 mt-1">{p.location}</p></div></div><div className="p-4 flex items-center justify-between"><span className="text-xs uppercase tracking-wider text-neutral-400">{translateCat(p.category)}</span><Link href={`/projects/${p.slug}`} className="text-gold text-sm font-medium hover:text-gold-light transition">التفاصيل →</Link></div></motion.div>))}</div>); }
function translateCat(c: Project['category']) { switch (c) { case 'residential': return 'سكني'; case 'commercial': return 'تجاري'; case 'infrastructure': return 'بنية تحتية'; case 'industrial': return 'صناعي'; case 'maintenance': return 'صيانة'; } }